# 2026-05-26

## 完成项
- 定位并确认易来新增情景“公区全关”，实体为 `button.yp_scene_916502`。
- 更新 `~/homeassistant/configuration.yaml`，将该实体加入 HomeKit 主桥 `HA Selected Home` 的 `filter.include_entities`。
- 为该实体补充 HomeKit 显示名：`公区全关`。
- 重启 `homeassistant` Docker 容器完成配置生效验证。
- 记录用户新偏好：内部操作默认直接执行；如系统权限机制强制拦截，再走批准流程。

## 未完成项
- 等待用户在 Home App 中确认新情景是否已正常显示并可触发。

## 明日第一步
- 若用户反馈 HomeKit 中未出现“公区全关”，优先检查 HomeKit bridge 同步状态、缓存刷新与实体暴露是否被客户端延迟更新。
