# 2026-03-25 日报

## 今日完成

### 1) Home Assistant：娱乐 Dashboard 迭代
- 新增“娱乐”Dashboard，完成音乐专辑封面卡片化展示，并支持点击封面触发 HomePod 播放。
- 处理启动故障：定位并修复 `configuration.yaml` 中无效 `media_dirs` 路径导致的 HA 启动失败问题，服务恢复正常。
- 按要求将播放链路改为 **MA 元数据播放**，避免本地同步大文件占用磁盘。
- 完成卡片统一尺寸（2x2 方形网格）与视觉样式优化。
- 封面升级为“真实专辑封面图”，替换掉手绘 CD 风格。

### 2) Home Assistant：广播 Dashboard 新建
- 新增“广播”Dashboard（侧边栏可见）。
- 预置常用广播按钮：吃饭了 / 起床了 / 睡觉了 / 准备出门 / 回家了 / 开会提醒。
- 新增“自定义广播内容”输入框 + 发送脚本按钮。
- 修复“未找到实体”问题：更正 package 文件结构后恢复 `input_text.broadcast_message`。
- 广播页改为 Mushroom UI 风格，提升可用性与视觉一致性。

### 3) RSS Reader：IMA 一键保存功能
- 在 RSS Web 页“查看原文”旁新增“保存到 IMA”按钮，后续简化为极简「+」按钮。
- 后端新增接口 `/api/ima/save-url`（兼容 `/rss/api/ima/save-url`），实现把原文 URL 直接写入 IMA 知识库。
- 修复线上报错：
  - `/rss` 前缀路由调用错误；
  - 前端 JSON 解析报 `is not a valid json`；
  - 后端 `Request` 命名冲突。
- 已在阿里云完成构建与部署，并实测保存 URL 返回成功（HTTP 200）。

### 4) RSS 配置保护与部署流程加固
- 响应“部署不覆盖已重配 RSS 源”要求，新增安全部署脚本：
  - `/opt/stanley-rss-reader/deploy_safe.sh`
  - 部署时先备份再恢复 `rss_sources.json`。
- 代码结构升级：
  - 仓库内改用 `rss_sources.json.template`；
  - 仅当 `rss_sources.json` 不存在时自动复制生成。
- 防止后续代码更新覆盖服务器本地 RSS 源配置。

### 5) 密钥管理标准化
- 建立统一密钥本：`/home/guosq/.openclaw/workspace/.secrets/keybook.json`。
- 采用 JSON 管理，权限设置为 `600`。
- 已纳入 IMA、Home Assistant、Music Assistant、网易云等关键凭据。
- 约定后续查找密钥默认先从密钥本读取。

---

## 今日未完成 / 待推进
1. 将娱乐页剩余卡片统一改为“search 后播”的稳健模式（减少名称匹配失败概率）。
2. 为广播页补充轻量反馈（如 toast 或按钮状态闪烁）而不增加视觉噪音。
3. 清理阿里云 RSS 部署中偶发 8090 端口占用流程，收敛为单命令稳定发布。

---

## 明日第一步
先做一次阿里云 RSS 的“无中断发布”脚本化收敛（拉代码、保留配置、重启、健康检查一体化）。

---

## 今日小结
今天核心是“从可用到稳定”：HA 端完成娱乐/广播双面板落地并修复关键故障；RSS 端实现 IMA 一键保存并完成多轮线上修复；同时把配置与密钥管理流程标准化，降低后续运维风险。