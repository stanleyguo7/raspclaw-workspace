# 2026-03-27 日报

## 今日完成

1. **网盘挂载与同步排查**
   - 检查了 Quark/Baidu 挂载状态：两者挂载正常、服务运行正常。
   - 定位到 `backup-to-quark.service` 失败根因：两个受限文件权限导致 `permission denied`。
   - 验证了修复思路：为备份命令增加排除项后，`rclone sync --dry-run` 可正常通过。

2. **AList / 网易云音乐接入与验证**
   - 确认 AList 里存在网易云 storage：`/pan/netease_music`（驱动 `NeteaseMusic`）。
   - 补充了 rclone remote：`alist_netease_music:`（WebDAV 到 `/dav/pan/netease_music`）。
   - 挂载到本地：`/home/guosq/cloud/pan/netease-music`。
   - 配置 systemd 自启动：`rclone-netease-mount.service`（已 enable）。

3. **Music Assistant 本地文件源配置**
   - 调整 MA 容器挂载，新增：
     - `/media/quark` -> `/home/guosq/cloud/pan/quark`
     - `/media/netease` -> `/home/guosq/cloud/pan/netease-music`
   - 配置并启用 filesystem local providers：
     - `Quark Music`：`/media/quark/music`
     - `Netease Music`：`/media/netease`
   - 状态校验：provider 已启用且 `last_error=None`。

4. **歌单与本地整理**
   - 将网易云歌曲整理到本地 MA 目录：`/home/guosq/musicassistant/media/163Music`（41 首）。
   - 生成可导入歌单：
     - `quark-all.m3u`（421 首）
     - `netease-all.m3u`（41 首）
     - `all-cloud-music.m3u`（462 首）
   - 受 AList WebDAV 405/500 影响，暂未实现“网盘侧自动写回歌单 + 程序化自动导入”。

## 未完成 / 风险

1. `backup-to-quark.service` 仍需把排除规则落盘到系统脚本（`/usr/local/bin/backup-to-quark.sh`），并重启验证。  
2. AList 对部分写操作返回 405/500（尤其 copy/move/copyto），影响网盘侧目录重组与歌单自动回写。  
3. MA 端“自动导入 m3u”没有找到稳定 API 入口，当前仍建议在 UI 进行一次导入。

## 明日第一步

1. 先完成 `backup-to-quark.sh` 的排除规则落盘并触发一次手动备份，确认 service 恢复为 `active (exited)`。  
2. 对 AList 网易云 storage 做一次“写能力最小化测试”（mkdir/touch/copyto/move），确定可用写路径与限制，再决定歌单自动化方案。  
3. 若 API 仍不稳定，落地“本地目录定时重建歌单 + UI 一键导入”的半自动流程说明，确保日常可用。
