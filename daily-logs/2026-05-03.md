# 2026-05-03 工作日志

## 昨日完成

1. 排查并解决 Home Assistant Lovelace dashboard 已更新但前端不显示的问题。
   - 确认 `.storage/lovelace.dashboard_home` 与 `.storage/lovelace.dashboard_daping` 文件内容已正确写入。
   - 判断问题不在文件落盘，而在 Home Assistant 前端 / App 侧未重新拉取 storage dashboard 配置。
   - 通过重启容器 `docker restart homeassistant` 后，新的 dashboard 标题与简洁版 UI 正常生效。

2. 沉淀出后续排查该类问题的优先检查路径。
   - 检查 `.storage/lovelace_dashboards` 注册项是否正确。
   - 确认当前 dashboard 是否属于 storage dashboard。
   - 如文件已更新但界面无变化，优先直接重启 HA 容器触发前端重新加载。

3. 持续优化 Home Assistant 首页 dashboard 设计方向。
   - 明确首页 `dashboard_home` 走“简洁、深色、偏高级、手机端友好”的界面风格。
   - 统一优先使用 Mushroom 风格卡片、图标与控件，尽量减少非 Mushroom 控件。
   - 页面结构进一步统一：
     - 公区页面按子区域清晰分组；
     - 房间页面统一为“情景模式 → 主要灯光 → 窗帘”的顺序。

4. 明确 HomeKit bridge 的暴露策略。
   - 用户确认 HomeKit 侧不仅要保留设备本体，还需要保留“情景”。
   - 后续默认采用“主设备 + 高频情景”的精选暴露策略。

5. 完成海康摄像头接入链路配置与验证。
   - 为海康摄像头 `192.168.3.85`（用户名 `admin`）配置 go2rtc。
   - 验证主码流与子码流均可用。
   - 统一命名：
     - `hikvision_85_sub`：子码流，预览优先；
     - `hikvision_85_main`：主码流，高清 / 分析优先。

6. 完成摄像头在 Home Assistant 中的接入与首页展示。
   - 通过 go2rtc 暴露的 RTSP 地址创建 `camera.hikvision_85_main` 与 `camera.hikvision_85_sub`。
   - 已将子码流预览卡加入 `dashboard_home` 首页。
   - 同时记录经验：后续 dashboard 或新摄像头更新后若前端未即时显示，优先记得重启 HA 容器刷新。

## 产出 / 结论

- 昨天的核心结果是：把“HA dashboard 文件已更新但界面不显示”的问题定位到了前端/配置刷新链路，而不是文件本身；并通过重启 Home Assistant 容器完成修复。
- 同时推进了 HA 首页设计规范、HomeKit 情景保留策略，以及海康摄像头接入与首页展示。

## 明日可继续

- 继续按照既定风格完善 `dashboard_home` 其他页面与卡片布局。
- 若接入更多摄像头，沿用 go2rtc 当前命名和接入方式。
- 后续维护 HomeKit bridge 时，继续按“主设备 + 高频情景”策略整理暴露项。
