## 2026-06-03

### 事项记录
- 清晨协助排查局域网机顶盒 `192.168.3.40` 的 IPTV 播放源抓取方式。
- 探测到目标主机在线、可 ping，`8080` 端口开放，但直接探测常见 HTTP/portal 路径均超时，暂不判断为可直接读取的播放列表接口。
- 已编写抓取脚本：`scripts/capture_iptv_source.sh`，用于在机顶盒播放频道时抓取其网络流量并提取可能的 `m3u8/ts/rtsp/udp` 源地址。
- 当前待用户在机顶盒上打开 IPTV 频道后，再执行脚本进行实际抓取。

### 当前结论
- 现阶段最可行方案是“播放时抓包提取真实源地址”，而不是直接枚举盒子上的固定接口。

### 明日第一步
- 等用户打开频道后，运行 `scripts/capture_iptv_source.sh 192.168.3.40 90`，检查 `urls.txt` 与 `summary.txt` 是否提取到真实播放源。
